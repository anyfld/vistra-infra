# go2rtc configuration for WebRTC 1:N streaming
streams:
  camera: []

webrtc:
  candidates:
    - ${LOCAL_IP}:8555
  ice_servers:
    - urls: ['stun:stun.l.google.com:19302']

api:
  origin: "*"
  listen: ":1984"
  tls_listen: ":443"
  tls_cert: /certs/local.pem
  tls_key: /certs/local-key.pem
